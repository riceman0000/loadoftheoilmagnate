using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;
using UnityEngine.EventSystems;

public class anytest : MonoBehaviour
{
    //private void Update()
    //{
    //    if (Input.GetMouseButtonDown(0))
    //    {
    //        var ped = new PointerEventData(EventSystem.current)
    //        {
    //            position = Input.mousePosition
    //        };

    //        var raycastResults = new List<RaycastResult>(); // UI用レイキャストの結果をリストで保持する変数
    //        EventSystem.current.RaycastAll(ped, raycastResults); // ここでレイキャストを飛ばして当たったオブジェクトをリストに格納する

    //        //foreach (var res in raycastResults)
    //        //{
    //        //    Debug.Log(res.gameObject.name);
    //        //}
    //        if(raycastResults.Any())
    //        {
    //            Debug.Log(raycastResults.First().gameObject.name);
    //        }
    //    }
    //}
}
